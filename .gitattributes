# Default to LF in the repo (good for Linux/Heroku)
* text=auto eol=lf

# Keep Windows-native endings where they matter
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Force LF where Heroku/Linux care
*.sh text eol=lf
Procfile text eol=lf
